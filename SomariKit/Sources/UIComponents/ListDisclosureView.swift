import SwiftUI

public struct ListDisclosureView: View {
    public var body: some View {
        Image(systemName: "chevron.right")
            .foregroundStyle(.black.opacity(0.6))
    }

    public init() {
    }
}
